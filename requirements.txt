# Core platform
streamlit>=1.35.0
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
plotly>=5.18.0
matplotlib>=3.7.0
requests>=2.31.0

# Live feed — Angel One SmartAPI (free, real-time tick data)
# Install: pip install smartapi-python pyotp
# smartapi-python>=1.3.3
# pyotp>=2.9.0

# Live feed — Fyers API v3 (free, real-time + auto order execution)
# Install: pip install fyers-apiv3
# fyers-apiv3>=3.0.0

# Optional: fake user agent for NSE session (helps avoid blocks)
# pip install fake-useragent
